server:
  port: 8992

spring:
  application:
  # 不能有下划线，不区分大小写
    name: HYSTRIXCLIENT
  cloud:
    consul:
      host: localhost
      port: 8500



#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:8761/eureka

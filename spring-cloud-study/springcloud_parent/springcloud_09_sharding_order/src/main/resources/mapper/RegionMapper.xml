<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zf.mapper.RegionMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.zf.entity.Region">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="code" property="code" jdbcType="VARCHAR"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="parent_id" property="parentId" jdbcType="BIGINT"/>
        <result column="level" property="level" jdbcType="INTEGER"/>
        <result column="sort" property="sort" jdbcType="INTEGER"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
        <result column="deleted" property="deleted" jdbcType="INTEGER"/>
    </resultMap>

    <!-- 基础列 -->
    <sql id="Base_Column_List">
        id, code, name, parent_id, level, sort, create_time, update_time, deleted
    </sql>

    <!-- 插入区域 -->
    <insert id="insert" parameterType="com.zf.entity.Region" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_region (code, name, parent_id, level, sort, create_time, update_time, deleted)
        VALUES (#{code}, #{name}, #{parentId}, #{level}, #{sort}, #{createTime}, #{updateTime}, #{deleted})
    </insert>

    <!-- 根据ID查询区域 -->
    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM t_region
        WHERE id = #{id} AND deleted = 0
    </select>

    <!-- 查询所有区域 -->
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM t_region
        WHERE deleted = 0
        ORDER BY sort ASC, id ASC
    </select>

    <!-- 统计区域数量 -->
    <select id="selectCount" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM t_region
        WHERE deleted = 0
    </select>

    <!-- 根据ID更新区域 -->
    <update id="updateById" parameterType="com.zf.entity.Region">
        UPDATE t_region
        <set>
            <if test="code != null">code = #{code},</if>
            <if test="name != null">name = #{name},</if>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="level != null">level = #{level},</if>
            <if test="sort != null">sort = #{sort},</if>
            <if test="deleted != null">deleted = #{deleted},</if>
            update_time = #{updateTime}
        </set>
        WHERE id = #{id}
    </update>

</mapper>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zf.mapper.OrderItemMapper">

    <!-- 批量插入订单明细 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO t_order_item (order_id, product_id, product_name, price, quantity, 
                                 total_amount, create_time, update_time, deleted)
        VALUES
        <foreach collection="items" item="item" separator=",">
            (#{item.orderId}, #{item.productId}, #{item.productName}, #{item.price}, 
             #{item.quantity}, #{item.totalAmount}, #{item.createTime}, #{item.updateTime}, 
             #{item.deleted})
        </foreach>
    </insert>

</mapper>